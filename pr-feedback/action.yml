name: 'PR Feedback'
description: 'Takes Feedback from LLM reagrding the PR created.'
inputs:
  project-id:
    description: 'The id of the GCP Project.'
    required: true
  location:
    description: 'The location of the GCP Project.'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    PROJECT_ID: ${{ inputs.project-id}}
    LOCATION: ${{ inputs.location }}